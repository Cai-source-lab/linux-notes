#部署docker，适配本机环境

##1.docker images
作用：查看本地有哪些镜像（ubuntu / busybox / hello-world

2.docker run ubuntu
作用：拉取镜像（隐式发生）,创建并运行容器.如果本地没有，会自动 pull

3.docker run -it ubuntu bash
作用：交互式进入容器，
-i：可交互
-t：终端
bash：进入 shell

4.docker run -it --name my-first-ubuntu ubuntu bash
作用：创建并命名容器，给容器固定身份

5.docker run --rm ubuntu
作用：自动删除容器（避免垃圾）

6.docker ps
作用：查看正在运行的容器

7.docker ps -a
作用：查看全部容器（包括停止的）

8.docker start my-first-ubuntu
                                     （ 容器名字）
作用：启动容器

*9.docker start -ai my-first-ubuntu
作用：启动并进入容器

10.docker exec -it my-first-ubuntu bash
作用：进入正在运行中的容器

11.docker rm <container_id>
作用：删除单个容器

12.docker rm <container_id>
作用：强制删除（运行中也删），慎用

13.docker container prune -f
作用：清理所有停止容器

14.docker image prune -a -f
作用：删除无用镜像

15.docker run hello-world
作用：容器测试，验证docker

16.docker run busybox echo "OK"
busybox（轻量工具）

*核心理解
1️⃣ run = 创建新容器
每次都是新系统
2️⃣ start = 启动旧容器
恢复之前的环境
3️⃣ exec = 进入容器
进入正在运行的系统

###问题发现：
1. Docker 无法拉取镜像
问题
failed to connect registry-1.docker.io
connection refused
原因
虚拟机 NAT 网络可用，但无法直接访问 Docker Hub
DNS/IPv6 路径异常
未配置代理到 Docker daemon
解决过程
测试外网连通性
ping 8.8.8.8
测试 Docker Hub
curl https://registry-1.docker.io
在 Windows Clash 开启代理
VM 指向 Windows 代理地址
配置 Docker daemon proxy
Environment="HTTP_PROXY=http://10.175.42.214:7890"
Environment="HTTPS_PROXY=http://10.175.42.214:7890"
结果

Docker 镜像可以正常拉取


2. 容器内无法访问外网
问题
curl https://www.google.com

无响应或失败

原因

容器没有继承宿主机代理环境变量

解决

在容器内设置：

export http_proxy=...
export https_proxy=...


3. Docker 命令混乱
问题

混用 run / start / exec

原因

没有区分三者作用

结论
run：创建新容器
start：启动已有容器
exec：进入运行中的容器


4. 容器数量过多
问题

大量 exited 容器堆积

原因

每次 run 都创建新容器

解决
docker container prune -f


5. docker start 使用错误
问题
docker start my-first-ubuntu bash
原因

start 不接受 bash 参数

正确方式
docker start -ai my-first-ubuntu

或

docker start my-first-ubuntu
docker exec -it my-first-ubuntu bash


6. 容器内没有 docker 命令
问题
docker: command not found
原因

Docker daemon 在宿主机，不在容器内

处理

退出容器，在宿主机执行 docker 命令


7. apt 安装 curl 问题
问题

curl 不可用或安装异常

原因
容器环境精简
未执行 apt update
网络依赖代理
解决
apt update
apt install curl -y


8. 容器生命周期理解问题
问题

不清楚容器是否“保存环境”

结论
run：创建新环境
start：恢复旧环境
exec：进入环境

