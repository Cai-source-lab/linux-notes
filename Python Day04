##1. Python基础脚本（日志分析初版）
(1) 文件读取（open + readlines）
作用：读取 log.txt 文件内容
解释：
open() 用于打开文件
"r" 表示只读模式
readlines() 将文件按行读取为列表

(2) 行遍历（for循环）
作用：逐行处理日志内容
解释：
for line in lines 表示一行一行读取日志
每一行都是一个字符串

(3) 字符串处理（lower + strip）
作用：统一格式，避免统计错误
解释：
lower() → 全部转小写（避免 ERROR / error 不一致）
strip() → 去掉换行符和空格
##2. 关键词统计逻辑（dict版本升级）

(1) defaultdict(int)
作用：自动初始化计数器
解释：
不需要手动写 if 判断
默认值为 0

(2) 多关键词统计
作用：统计多个关键词出现次数
解释：
使用 dict 存储关键词
for key in keywords 遍历所有关键词
if key in line 判断是否命中
result[key] += 1 进行累加

(3) 逻辑结构理解
作用：构建日志分析核心流程
解释：
外层循环：遍历每一行日志
内层循环：遍历所有关键词
判断 + 计数 → 完成统计

##3. Linux命令行参数（sys.argv）
(1) sys.argv
作用：接收命令行输入文件
解释：
sys.argv[0] = 脚本名
sys.argv[1] = 输入文件

(2) 参数校验
作用：防止用户未输入文件
解释：
len(sys.argv) != 2 → 提示用法
sys.exit(1) → 退出程序
##4. 异常处理（try / except）

(1) 文件异常捕获
作用：防止文件不存在导致程序崩溃
解释：
FileNotFoundError 捕获文件不存在错误
提示用户错误信息

##5. 输出优化（report.txt）
(1) 写入文件（with open）
作用：生成分析报告
解释：
"w" = 写模式（覆盖文件）
自动创建 report.txt

(2) 换行符 \n
作用：控制文件格式
解释：
\n 才是换行
/n 是错误写法（不会换行）

(3) report.txt内容生成
作用：保存统计结果
解释：
使用 f.write 写入文件
遍历 dict 输出所有统计结果


##6. JSON报告升级（进阶内容）
(1) json.dump
作用：生成结构化数据文件
解释：
Python dict → JSON 文件
机器可读格式

(2) indent参数
作用：美化JSON格式
解释：
indent=4 → 缩进4格
提高可读性

(3) ensure_ascii=False
作用：支持中文输出
解释：
防止中文变成 Unicode 编码

### 今日问题发现与处理
问题1：缩进错误导致程序报错
现象：IndentationError
原因：if / for / try 结构后未正确缩进代码块
处理：统一使用四空格缩进，明确代码块层级

问题2：换行符错误
现象：report.txt内容全部挤在一行
原因：误写 /n 而非 \n
处理：统一修正为 \n

问题3：拼写错误
现象：FileNotFoundError 未捕获 / keywords变量错误
原因：变量名拼写不一致或错误
处理：统一变量命名规范，检查大小写与拼写

问题4：循环结构错误
现象：统计结果不正确或遗漏
原因：内外层循环缩进错误导致逻辑断裂
处理：明确结构：
外层：逐行遍历
内层：关键词匹配
